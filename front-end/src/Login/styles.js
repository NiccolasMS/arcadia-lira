import * as React from "react";

const useStyles = () => ({
  fundo: {
    backgroundImage: "linear-gradient(#FFFFFF, #FFFFFF, #FFDBC1, #FFB179)",
    width: "100vw",
    height: "100vh",
  },
});

export default useStyles;
